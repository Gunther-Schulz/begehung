# Handoff — five begehung amendments from two production rounds (2026-08-26)

Judgment desk: `claude-code-cache-fix-9e [96132d]` (fable). Peer desk:
`begehung-c7` (opus), delegation confirmed first-hand by the operator in
that session before work starts. REPORT-CHANNEL: SendMessage
`claude-code-cache-fix-9e [96132d]`; one digest per amendment landed;
blockers immediately; the begehung sha on every message. Re-read this
file at HEAD before each self-review dispatch.

## The brief
`dev-notes/OBSERVATIONS.md`, entry "2026-08-26 — two production rounds
(3 and 4) on one design: five improvements" — the five items ARE the
work list, in that order. Provenance: cache-fix `BEGEHUNG-MAP.md` rows
50 and the round-4 record; the round-3/4 walkers' reports live in the
judgment desk's session only (not on disk) — the entry's own text is
the grounding, not a pointer to be resolved.

## Process (skill-craft governs; begehung's own CLAUDE.md binds)
- Load `skill-craft:skill-craft` in every turn that edits SKILL.md or
  references (the pre-edit gate demands it).
- Amendment over addition: each item first searches the governed set
  (SKILL.md + references) for the concept — the placement scan
  (command + hits) is the edit's basis and goes in the digest.
- Fire-born machinery: the five carry two rounds' provenance; no other
  gate or mandate is added.
- Every change dispatches one fresh-context self-review before commit
  (`references/self-review.md`), findings dispositioned, then commit
  with the `Skill-reviewed:` trailer; version bump per plugin
  convention; release via `/release-plugin` is the OPERATOR's act
  (outward) — stop before it and report.
- Item 1 (findings as a DATA file entering a carrier via intake) has a
  dependency: the intake tool is the lifecycle plugin
  (`~/dev/Gunther-Schulz/lifecycle`, read-only for you), whose
  `item add --source` exists today; the amendment names the file shape
  and the pointer-plus-counts message; it does not build the intake.
- Write set: the begehung repo only. Commit by pathspec, never amend,
  never `--no-verify`. Trailer: `Co-Authored-By: Claude Opus 5
  <noreply@anthropic.com>`.

## Done-criterion
Five amendments landed and self-reviewed, each digest carrying the
placement scan, the review's dispositions and the diff summary; the
observations entry's items marked ANGEWANDT/applied with commit refs;
a closing digest (a)–(h).
