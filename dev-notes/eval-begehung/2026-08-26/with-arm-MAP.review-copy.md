# Begehung map — statiker (skill + tools + run process)

System under review: the statiker development-loop system — SKILL.md
(operational text), the record/git/emit tools, the run record
grammar, and the repo's own carriers. Declared revisit interval:
one round per meta-session week while the trial runs; a row
unvisited longer is a finding (skill rule).

Derivation, first run 2026-08-16: rows from claim-emission
surfaces (every place the system produces claims, values, or
verdicts), each crossed with the consumer where wrongness lands.
Failure dimensions from the loaded operator corpus (label-vs-body
drift, zero-hit-as-absence, self-reference, staleness, restated
basis, silent degradation) plus the trial's own measured class
(non-contraction).

| axis (what against what) | status | last visited (date · round) | yield | next step |
|---|---|---|---|---|
| SKILL.md conduct text vs desk behavior (does the text produce the conduct it claims) | prose-covered (trial relay grading, CLAUDE.md conventions; opus skill-edit review per release) | 2026-08-15 · trial relays 1–8 | high (continuous) | keep: the trial IS this row's instrument |
| Record-tool verdicts vs the defects they gate (false-clean / false-hold) | mechanically-guarded (tools/ suite 380, test_contract.py reach detector; red-first battery convention) | 2026-08-15 · lane D | high (F140/F147/F217 fire-tested) | next unfired verdict: E-P drives GATE_UNREADABLE red (READY, booked) |
| Tracker grammar vs ATTACKER consumption (filter artifact fidelity, line-number identity) | mechanically-guarded (filter blanking-in-place battery; F154 incident-tested) | 2026-08-15 · run rounds A8–A9 | med | none owed now |
| Tracker grammar vs SUCCESSOR-DESK consumption (resume path) | prose-covered (resume passage; record gate at resume minted 8fe5562) | 2026-08-16 · this session | high | validation criterion logged in OBSERVATIONS (first resume over holds) |
| Tracker grammar vs CROSS-SKILL consumers (/clippy-stats contract) | prose-covered — admission half mechanically pinned (record tool source-read, 0.12.4); metrics half measured DIVERGENT, claim narrowed in SKILL.md 0.2.70, reader-side booked in coding-clippy BACKLOG, residual decision parked (P11) | 2026-08-16 · R1 | 3 findings (2 executed divergences + 1 overwide claim) | P11's trigger: trial-close grading design |
| Seals/queues/comparisons (XDG state) vs successor desks | prose-covered (seal-path derivation verdict, P1 shipped; queue-spent grammar) | 2026-08-15 · lane D | med | none owed now |
| Certified-attack register (defaults/models) vs tier resolution at a real desk | prose-covered (SKILL.md:1045-1057) — effect probe RUN 2026-08-26: four live resolutions through the shipped register, beat-the-books 2026-08-16-canonical-frame-sign-repair tracker :300/:362/:401/:440, pre-registered pass signal (OBSERVATIONS:4406-4408) MET. SURPLUS MODELLED: the undispatchable-entry fall-through and the uncertified-repo-config branch are never exercised | 2026-08-26 · R4 | 8 findings (3 HIGH incl. this row's own refuted status) | land R4's six mechanism dispositions; the two unexercised branches stay the row's dark remainder |
| Implement/verify/close sections vs real execution | STATUS CORRECTED 2026-08-26 (R4, status-only re-read — axis NOT yet walked): no longer dark by construction. A unit has landed (frame-sign-repair tracker :468, `unit U1 landed: 07acf5a9`) and verify ran to `- V1 [PASSED]` (:481) with its own executed output | never walked (status re-read 2026-08-26 · R4) | — | NOW WALKABLE — the rotation's darkest readable candidate for R5 |
| Plugin/version pin vs served conduct | mechanically-guarded (plugin-drift-scan banner; payload-bump pre-commit hook — both fired this session) | 2026-08-16 · this session | med | none owed now |
| PLAN.md (design record) vs SKILL.md (shipped text) staleness | prose-covered — append-only supersession discipline exercised (R2 post-plan entry); no mechanical guard is buildable (the diff is judgment-shaped), backstop declared: this map's rotation re-walks the row | 2026-08-16 · R2 | 5 findings | re-walk after the trial-close grading (P11's trigger moment touches the same sections) |
| Repo carriers (BACKLOG/OBSERVATIONS) vs drain reality (capture-vs-drain ratio) | prose-covered (corpus retirement trigger; banner where deployed) | 2026-08-16 · this session (P9/P10 drained same-day) | low | ratio re-read at next session start |
| Relay channel (desk report → operator → meta) vs verbatim fidelity | prose-covered (two-session convention: verbatim relay, provenance trace) | 2026-08-15 · relays | med | none owed now |
| Run continuity vs harness turn/wake machinery (turn-ends, waits, wakers, dispatch lanes) | prose-covered (0.2.81 turn-end states clause; horizon rule) — mechanical half booked unbuilt (P16); stall waker-gap parked (P17) | 2026-08-16 · R3 | 2 (+2 prior mints today) | re-walk after P16 red-fires or P17's trigger |

## Round log

- 2026-08-16 · R1 — row: tracker grammar vs /clippy-stats
  (cross-skill consumer). Why: darkest executable row — the
  shared-ledger claim in SKILL.md:135 is a co-consumer contract
  never executed against post-churn trackers (the blast-radius
  class the skill's own attack block names); the two other dark
  rows are either not walkable by reading (implement/verify) or
  lower-stakes (register, PLAN diff). Registered before first
  search. CLOSED same day, yield 3: (1) cycle count `^## Cycle`
  = 15 for 13 actual cycles (misses "## Re-derivation after the
  attacks (cycle 3)", counts repair/correction/cont. headings) —
  control on clippy-era f2-min-odds counts as designed; (2)
  decision-family metric collapses to ONE family on numeric IDs,
  falls-per-entry 56/1 rendered as a measurement; (3) SKILL.md's
  shared-ledger sentence asserted a wider contract than is
  maintained — the label-vs-body class on the skill's own text.
  Dispositions all structural: claim narrowed (SKILL.md 0.2.70),
  reader-side branch booked (coding-clippy BACKLOG), residual
  decision parked (statiker P11). Coverage after R1: 3
  mechanically-guarded / 6 prose-covered / 3 dark of 12 rows.
  ACROSS-ROWS read: the three dark rows and R1's finding share
  one minting structure — claims never executed against their
  consumer because ALL trial evidence to date comes from ONE
  monolith run that never left the design phase; the structural
  cure is already booked (P9's per-unit re-seed: many small
  end-to-end runs exercise implement/verify/close and the
  register for free). Next round (rotation): implement/verify/
  close row — walkable only by the first landing run; until
  then the darkest READABLE row is PLAN.md-vs-SKILL.md
  staleness.

- 2026-08-16 · R2 — row: PLAN.md (design record) vs SKILL.md
  (shipped text) staleness. Why: rotation rule — darkest
  readable row (implement/verify stays unwalkable until a run
  lands; the register row is lower-stakes); PLAN.md is the
  re-derivation source CLAUDE.md names ("settled decisions are
  not re-opened without new evidence"), so a stale settled
  decision there misdirects any future session that reads it as
  current. Both directions walked: PLAN decisions the shipped
  text dropped or contradicts, and PLAN sections the shipped
  reality has overtaken. Registered before first search.
  CLOSED same day, yield 5, all dispositioned structurally:
  (1) Record-compatibility section's metric half refuted by R1's
  measurement — post-plan supersession entry appended (the
  append-only precedent: the GitHub-remote entry); (2) Settled
  bullet "attack tier: fable-first" contradicted by the section's
  own 2026-08-06 certification four lines up — named in the same
  entry; (3) Size-target hard reading superseded by the
  target-not-gate birth-class posture, previously recorded only
  outside PLAN — named in the same entry; (4) the clippy-side R1
  booking lacked the clippy-frozen trial constraint — freeze
  clause appended in coding-clippy BACKLOG (ebb9cb0); (5)
  process gap: releases 0.2.68–0.2.70 minted without the
  base-reference tenet-list check the 2026-08-05 tenet entry
  requires — check executed retroactively (all pass: early head
  round opens at the head's own stopped-moving seam; narrowing
  and scoped reopening keep the zero-delta circuit; no new human
  seat), and the mint discipline widened in CLAUDE.md so the
  check is recorded at mint time. Coverage after R2: 3
  mechanically-guarded / 7 prose-covered / 2 dark of 12 rows.
  ACROSS-ROWS read: both rounds' findings are one class —
  assurances written at a design moment and never re-executed
  against their consumer (R1: a cross-skill claim; R2: a design
  record's settled sections) — the structure minting it is
  churn outpacing its own record carriers, which the trial's
  release cadence guarantees; the standing cure is exactly this
  map's rotation until the cadence slows at stabilization.
  Next round (rotation): certified-attack register row (darkest
  readable); implement/verify/close stays waiting on the first
  landing run.

- 2026-08-16 · R3 — row: run continuity vs harness turn/wake
  machinery (NEW row, operator-named lens: the stop/turn-end
  seam, after two improvised desk stops today; overrides the R2
  rotation pointer per the skill's operator-lens rule).
  Registered before first search. CLOSED same day, yield 2
  beyond the day's prior mints (which the row inherits as
  covered ground: 0.2.79 ask clause, 0.2.81 turn-end states,
  P16 booked): (1) dispatch-lane harness binding drifted
  PER-SESSION — the meta session's Agent tool lost `name`/
  mailbox mid-session while the desk's kept both (desk
  counter-probe executed, A3 spawned "via mailbox" in the same
  window) — a session cannot infer another's lane from its own,
  and a mailbox channel line composed by a sync-only session
  strands the report; booked in the owning instrument's repo
  (dispatch-guards dev-notes, cc31e1e + bf9aea4, pre-formulated
  fix: per-session schema probe replaces the dated binding).
  (2) The mailbox wait's waker IS the monitored party: the
  horizon rule is prose only a woken desk can execute, a dead
  attacker = permanent stall indistinguishable from a long
  round, and P16's turn-end hook cannot cover a sleeper —
  parked as P17 with its missing evidence named (one observed
  stall past horizon; moot if sync-only becomes universal).
  Coverage after R3: 3 mechanically-guarded / 8 prose-covered /
  2 dark of 13 rows. ACROSS-ROWS read: R3's class differs from
  R1/R2's (claims stale against their consumer) — it is rules
  INERT AT THEIR FIRING MOMENT under momentum (two desk stops,
  the meta session's own B3 tenet-check skip and relay-grant
  miss, same day), and the recurring cure shape is the
  midturn-answer-check pattern: mechanize the computable core
  at the seam, leave the judgment edge prose (P16 is that cure
  for the widest seam; the operator's own hooks are the
  existence proof). Next round (rotation): certified-attack
  register row (darkest readable), unless a landing run opens
  implement/verify/close first.

## Round table (skill's current round-row form; rows R1–R3 back-filled from the prose log above, unfilled cells marked `—` = step not taken under the older prose form)

| round | date | axis | why that axis | read-at | closed-at | reach | class |
|---|---|---|---|---|---|---|---|
| 1 | 2026-08-16 | Tracker grammar vs CROSS-SKILL consumers (/clippy-stats) | darkest executable row | — | — | — | claims never executed against their consumer (all trial evidence from one monolith run) |
| 2 | 2026-08-16 | PLAN.md vs SKILL.md staleness | rotation: darkest readable | — | — | — | assurances written at a design moment, never re-executed against their consumer |
| 3 | 2026-08-16 | Run continuity vs harness turn/wake machinery | operator-named lens | — | — | — | rules inert at their firing moment under momentum |
| 4 | 2026-08-26 | Certified-attack register (defaults/models) vs tier resolution at a real desk | darkest row: `never` visited, and the explicit rotation pointer of BOTH R2's and R3's close reads. The equally-dark competitor (implement/verify/close vs real execution) is declared by the MAP itself not walkable by reading — the first landing run is its instrument. Registered before the round's first search. | 680cdba | 680cdba | 9 hold, 0 superseded | rows deferred to a future observation with no assigned harvester — spans axis rows `Tracker grammar vs SUCCESSOR-DESK consumption` (:22), `Certified-attack register` (:25), `Implement/verify/close sections vs real execution` (:26); property compared: whether the row's next step names an external observation, and whether any named party reads that observation back into the MAP |

REVIEW COPY — walker has no write path into the system under review
(read-only dispatch, 2026-08-26). This copy and the round's findings
file live in the walker's scratch; the persistent home stays
/home/g/dev/Gunther-Schulz/statiker/BEGEHUNG-MAP.md, landed by its
owner.

- 2026-08-26 · R4 — row: certified-attack register (defaults/models) vs tier
  resolution at a real desk. Why: darkest row (`never`), and the rotation
  pointer both R2 and R3 recorded. Registered before first search; read-at
  and closed-at both 680cdba (clean tree, zero intervening commits, so the
  close re-test found every finding standing: 9 hold, 0 superseded).
  CLOSED same day, yield 8 in-lens + 1 close row. THE ROUND'S CORE RESULT
  IS ABOUT THE MAP ITSELF: both status cells it re-read were REFUTED by the
  live run record. The register's resolution is not dark — it fired four
  times and its pre-registered pass signal resolved PASS, unharvested; and
  implement/verify/close is not unfired — U1 landed and V1 PASSED. Six
  further in-lens findings on the resolution text: absent undispatchable-
  model semantics contradicting a recorded operator decision; the
  'terminal fallback by construction' parenthetical exempting an
  uncertified repo config from the deviation duty; register classes no
  resolution path reads; no malformed-file route at the one seam that
  reads the register; provenance rendered without its blinding caveat;
  and every one of the 13 rows past the MAP's own declared one-week
  revisit interval. ACROSS-ROWS read: three rows defer validation to a
  future observation and assign no harvester — the MAP updates a row only
  when a round VISITS it, and deferral is the decision not to visit, so a
  deferred row can never be updated by the mechanism that maintains it.
  Structural cure (outranks three per-row patches): a deferred row carries
  a harvest trigger — observation, reader, when — on a read path that
  already runs, e.g. the per-relay obligations the repo CLAUDE.md already
  binds the meta session to on every desk report. Coverage after R4:
  3 mechanically-guarded / 10 prose-covered / 0 dark of 13 rows — with the
  caveat that 11 of those 13 status cells were NOT re-read this round, and
  the round refuted 2 of the 2 it did re-read. Next round (rotation):
  implement/verify/close, now walkable for the first time.
